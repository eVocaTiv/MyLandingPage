import React, { Component } from 'react';
import {
  VisionContainer,
} from '@containers';

class Vision extends Component {
  render() {
    return (
          <VisionContainer />
    );
  }
}

export default Vision;
